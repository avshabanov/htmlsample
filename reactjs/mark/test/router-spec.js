window = {};

var includeFile = require("./helper/include-file").includeFile;

describe("route should navigate to pages", function () {
    it("should navigate to profile page", function () {
        expect(1).toBe(1);
    });
});

